# UX evidence

## Changed surfaces

- First-launch ModePicker copy and local-mode defaults.
- Onboarding quick-start defaults.
- Local Whisper runtime installation error messaging.

## Screenshot requirement

The PR includes UI-copy/default changes but no new visual components. Reviewer should inspect:

- First-launch "Use locally" card copy.
- Onboarding AI step after choosing local mode.
- Local Whisper runtime error toast when runtime release assets are unavailable.

Screenshots should be added if the reviewer wants visual confirmation from a Windows/local install environment.

## Captured evidence

- `screenshots/after/mode-picker-local-copy.png` shows the updated first-launch copy explaining that local mode starts with on-device transcription and raw transcript output.
